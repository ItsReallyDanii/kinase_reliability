# Kinase Reliability Pilot v1.0 - Python Dependencies

# Core scientific computing
numpy>=1.24.0,<2.0.0

# JSON schema validation (optional, for SAR validation)
jsonschema>=4.17.0

# Structural biology tools (required for real metrics and structure ingestion)
# metrics/rmsd.py, metrics/contact_map.py, scripts/extract_ligands.py
biopython>=1.81
scipy>=1.10.0

# HTTP requests for structure download (stdlib urllib used instead; no extra dep needed)

# Development/testing
pytest>=7.3.0
